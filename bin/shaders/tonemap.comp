#version 460


layout(rgba32f, binding = 0) uniform image2D imgOut;

layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;
void main()
{
    ivec2 pixel = ivec2(gl_GlobalInvocationID.xy);

    imageStore(imgOut, pixel, vec4(1, 1, 1, 1));
}
